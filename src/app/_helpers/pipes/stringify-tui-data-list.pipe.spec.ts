import {StringifyTuiDataListPipe} from './stringify-tui-data-list.pipe'

describe('StringifyTuiDataListPipe', () => {
    it('create an instance', () => {
        const pipe = new StringifyTuiDataListPipe()
        expect(pipe).toBeTruthy()
    })
})
