export type LeaderboardElement = {
    rank?: number;
    name: string;
    token: number;
    member_names?: string[];
}

export type LeaderboardPageElement = {
    name: string;
    eventId: number;
}
